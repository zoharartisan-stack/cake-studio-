# lib/supabase

Supabase client factories (browser, server, service-role), auth helpers,
and generated DB types. Implemented in **Phase 2 — Database, Auth &
Multi-Tenancy**. Env vars are already declared in `.env.example`.
